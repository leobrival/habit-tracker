meta {
  name: Sign In
  type: http
  seq: 2
}

post {
  url: {{authUrl}}/sign-in
  body: json
  auth: none
}

body:json {
  {
    "email": "user@example.com",
    "password": "securePassword123"
  }
}

docs {
  Authenticate existing user and create session.

  Session cookie will be automatically set in browser.
}
